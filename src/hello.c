#include <stdio.h>      // need this for printf()

int main()              // main() returns an int type implicitly
{
        printf("Hello, world!\n");
        return 0;       // good style to return this int explicitly
}
